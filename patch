diff --git a/src-svelte/src/components/InputBar.svelte b/src-svelte/src/components/InputBar.svelte
index 923158a..6c9ea48 100644
--- a/src-svelte/src/components/InputBar.svelte
+++ b/src-svelte/src/components/InputBar.svelte
@@ -6,10 +6,11 @@
   import { completeNick } from '$lib/utils';
   import Send from '@lucide/svelte/icons/send';
   import { emojifyInput } from '$lib/emojify';
-  import { uploadImage } from '$lib/imgur';
   import Camera from '@lucide/svelte/icons/camera';
   import Megaphone from '@lucide/svelte/icons/megaphone';
   import { get } from 'svelte/store';
+  import ImageUploadPreview from '$components/ImageUploadPreview.svelte';
+  import { addToast } from '$lib/toast';
 
   let inputRef = $state<HTMLTextAreaElement>();
   let fileInputRef = $state<HTMLInputElement>();
@@ -17,6 +18,21 @@
   let _iterCandidate: string | null = $state(null);
   let isDraggingFile = $state(false);
 
+  interface PreviewItem {
+    id: number;
+    name: string;
+    size: number;
+    dataUrl: string;
+    progress: number;
+    status: 'preview' | 'uploading' | 'success' | 'error';
+    result?: { link: string; deletehash: string };
+    error?: string;
+  }
+
+  // Preview items for batch image upload modal
+  let previewItems = $state<PreviewItem[]>([]);
+  let nextPreviewId = $state(0);
+
   let canSend = $derived($currentBuffer && message.length > 0);
 
   function handleSend() {
@@ -265,46 +281,101 @@
     }
   }
 
-  async function handleFileUpload(file: File): Promise<void> {
-    try {
-      const result = await uploadImage(file, (percent) => {
-        // Progress callback — could show progress bar
-        void percent;
-      });
-      // Insert URL at cursor position
-      const caret = getCaretPos();
-      message = message.slice(0, caret) + result.link + message.slice(caret);
-      setTimeout(() => setCaretPos(caret + result.link.length), 0);
-    } catch (err) {
-      console.error('Image upload failed:', err);
+  // Read a single file as a data URL for preview
+  function readImageAsDataUrl(file: File): Promise<{ dataUrl: string; name: string; size: number }> {
+    return new Promise((resolve, reject) => {
+      const reader = new FileReader();
+      reader.onload = () => {
+        resolve({ dataUrl: reader.result as string, name: file.name, size: file.size });
+      };
+      reader.onerror = () => reject(new Error(`Failed to read file: ${file.name}`));
+      reader.readAsDataURL(file);
+    });
+  }
+
+  // Collect image files and open the preview modal
+  async function openPreviewModal(files: FileList | File[]) {
+    const imageFiles = Array.from(files).filter(f => f.type.match(/image.*/));
+    if (imageFiles.length === 0) {
+      addToast('No images found in files', { type: 'warning' });
+      return;
     }
+
+    // Read all images as data URLs for preview
+    const reads = await Promise.allSettled(imageFiles.map(f => readImageAsDataUrl(f)));
+
+    const items = [];
+    for (const r of reads) {
+      if (r.status === 'fulfilled') {
+        nextPreviewId++;
+        items.push({
+          id: nextPreviewId,
+          name: r.value.name,
+          size: r.value.size,
+          dataUrl: r.value.dataUrl,
+          progress: 0,
+          status: 'preview' as const,
+        });
+      } else {
+        addToast(`Could not read image: ${r.reason?.message}`, { type: 'error' });
+      }
+    }
+
+    if (items.length === 0) return;
+
+    previewItems = items;
+    // Open the popover modal
+    const dialog = document.getElementById('image-upload-preview');
+    dialog?.showPopover();
+  }
+
+  // Handle paste event — detect clipboard images and open preview
+  function handlePaste(e: ClipboardEvent) {
+    const clipboardFiles = e.clipboardData?.files;
+    if (!clipboardFiles || clipboardFiles.length === 0) return;
+
+    const hasImages = Array.from(clipboardFiles).some(f => f.type.match(/image.*/));
+    if (!hasImages) return;
+
+    e.preventDefault();
+    e.stopPropagation();
+    void openPreviewModal(clipboardFiles);
   }
 
+  // Insert uploaded URLs at cursor position in the input
+  function handleInsertUrls(urls: string[]) {
+    if (urls.length === 0) return;
+    const caret = getCaretPos();
+    const urlsText = urls.join(' ');
+    message = message.slice(0, caret) + urlsText + message.slice(caret);
+    setTimeout(() => setCaretPos(caret + urlsText.length), 0);
+    // Clear preview state after inserting
+    previewItems = [];
+    inputRef?.focus();
+  }
+
+  // Close preview modal and clear state
+  function handleClosePreview() {
+    previewItems = [];
+  }
+
+  // File picker now routes through preview modal
   function handleFileInputChange() {
     const files = fileInputRef?.files;
-    if (!files) return;
-    for (let i = 0; i < files.length; i++) {
-      const file = files[i];
-      if (file) {
-        void handleFileUpload(file);
-      }
-    }
+    if (!files || files.length === 0) return;
+    void openPreviewModal(files);
     if (fileInputRef) fileInputRef.value = '';
   }
 
+  // Drag-drop now routes through preview modal
   function handleDrop(e: DragEvent) {
     e.preventDefault();
     e.stopPropagation();
     isDraggingFile = false;
 
     const files = e.dataTransfer?.files;
-    if (!files) return;
-    for (let i = 0; i < files.length; i++) {
-      const file = files[i];
-      if (file && file.type.match(/image.*/)) {
-        void handleFileUpload(file);
-      }
-    }
+    if (!files || files.length === 0) return;
+    void openPreviewModal(files);
   }
 
   function handleDragOver(e: DragEvent) {
@@ -353,6 +424,7 @@
       bind:value={message}
       onkeydown={handleKeyDown}
       oninput={handleInput}
+      onpaste={handlePaste}
       ondrop={handleDrop}
       ondragover={handleDragOver}
       ondragleave={handleDragEnd}
@@ -393,3 +465,9 @@
     </div>
   </div>
 </div>
+
+<ImageUploadPreview
+  images={previewItems}
+  onInsert={handleInsertUrls}
+  onClose={handleClosePreview}
+/>
\ No newline at end of file
diff --git a/src-svelte/src/lib/debug.ts b/src-svelte/src/lib/debug.ts
index bd6ca34..e9c9275 100644
--- a/src-svelte/src/lib/debug.ts
+++ b/src-svelte/src/lib/debug.ts
@@ -1,3 +1,4 @@
 // Debug logging flags — flip these to re-enable console spam during debugging
 export const DEBUG_NICKLIST = false;
 export const DEBUG_WEECHAT_COMMANDS = true;
+export const DEBUG_BUFFER = true;
diff --git a/src-svelte/src/lib/imgur.ts b/src-svelte/src/lib/imgur.ts
index ad25857..56b614a 100644
--- a/src-svelte/src/lib/imgur.ts
+++ b/src-svelte/src/lib/imgur.ts
@@ -1,4 +1,5 @@
 import { settings } from '$lib/stores/settings';
+import { get } from 'svelte/store';
 
 const DEFAULT_CLIENT_ID = '164efef8979cd4b';
 
@@ -7,79 +8,117 @@ interface UploadResult {
     deletehash: string;
 }
 
+// Read iToken and iAlb from settings store synchronously.
+function getImgurSettings() {
+    const s = get(settings);
+    return { iToken: s.iToken || '', iAlb: s.iAlb || '' };
+}
+
+// Set the appropriate Authorization header on an XMLHttpRequest.
+function setAuthHeader(xhr: XMLHttpRequest) {
+    const { iToken } = getImgurSettings();
+    if (iToken.length >= 38) {
+        xhr.setRequestHeader('Authorization', 'Bearer ' + iToken);
+    } else {
+        xhr.setRequestHeader('Authorization', 'Client-ID ' + DEFAULT_CLIENT_ID);
+    }
+}
+
 export function uploadImage(
-    file: File | null,
+    fileOrBase64: File | string | null,
     progressCallback?: (percent: number) => void,
 ): Promise<UploadResult> {
-    if (!file || !file.type.match(/image.*/)) {
+    let base64Data: string;
+
+    if (typeof fileOrBase64 === 'string') {
+        // Already a base64 data URL or raw base64 string
+        base64Data = fileOrBase64.startsWith('data:') ? fileOrBase64.split(',')[1] || '' : fileOrBase64;
+    } else if (fileOrBase64 && fileOrBase64.type.match(/image.*/)) {
+        // File — need to read it first
+        return new Promise((resolve, reject) => {
+            const reader = new FileReader();
+            reader.onload = (event) => {
+                resolve(doUpload(event.target?.result as string, progressCallback));
+            };
+            reader.onerror = () => reject(new Error('Failed to read file'));
+            reader.readAsDataURL(fileOrBase64);
+        });
+    } else {
         return Promise.reject(new Error('File is not an image'));
     }
 
+    return doUpload(base64Data, progressCallback);
+}
+
+// Core upload logic given a base64-encoded image string.
+function doUpload(
+    base64: string,
+    progressCallback?: (percent: number) => void,
+): Promise<UploadResult> {
     return new Promise((resolve, reject) => {
-        const reader = new FileReader();
+        const xhr = new XMLHttpRequest();
+        xhr.open('POST', 'https://api.imgur.com/3/image', true);
 
-        reader.onload = (event) => {
-            const base64 = (event.target?.result as string).split(',')[1] || '';
+        setAuthHeader(xhr);
+        xhr.setRequestHeader('Accept', 'application/json');
 
-            const xhr = new XMLHttpRequest();
-            xhr.open('POST', 'https://api.imgur.com/3/image', true);
+        const { iToken, iAlb } = getImgurSettings();
+        const fd = new FormData();
+        fd.append('image', base64);
+        fd.append('type', 'base64');
 
-            // Auth header
-            let iToken = '';
-            let iAlb = '';
-            const unsubSettings = settings.subscribe((s) => {
-                iToken = s.iToken || '';
-                iAlb = s.iAlb || '';
-            });
-            unsubSettings();
+        if (iToken.length >= 38 && iAlb.length >= 6) {
+            fd.append('album', iAlb);
+        }
 
-            if (iToken.length >= 38) {
-                xhr.setRequestHeader('Authorization', 'Bearer ' + iToken);
+        xhr.onload = () => {
+            if (xhr.status === 200) {
+                try {
+                    const response = JSON.parse(xhr.responseText);
+                    if (response.data && response.data.link) {
+                        resolve({
+                            link: response.data.link.replace(/^http:/, 'https:'),
+                            deletehash: response.data.deletehash || '',
+                        });
+                    } else {
+                        reject(new Error('Upload failed: no link in response'));
+                    }
+                } catch {
+                    reject(new Error('Upload failed: invalid response'));
+                }
             } else {
-                xhr.setRequestHeader('Authorization', 'Client-ID ' + DEFAULT_CLIENT_ID);
-            }
-
-            xhr.setRequestHeader('Accept', 'application/json');
-
-            const fd = new FormData();
-            fd.append('image', base64);
-            fd.append('type', 'base64');
-
-            if (iToken.length >= 38 && iAlb.length >= 6) {
-                fd.append('album', iAlb);
+                reject(new Error('Upload failed: HTTP ' + xhr.status));
             }
+        };
 
-            xhr.onload = () => {
-                if (xhr.status === 200) {
-                    try {
-                        const response = JSON.parse(xhr.responseText);
-                        if (response.data && response.data.link) {
-                            resolve({
-                                link: response.data.link.replace(/^http:/, 'https:'),
-                                deletehash: response.data.deletehash || '',
-                            });
-                        } else {
-                            reject(new Error('Upload failed: no link in response'));
-                        }
-                    } catch {
-                        reject(new Error('Upload failed: invalid response'));
-                    }
-                } else {
-                    reject(new Error('Upload failed: HTTP ' + xhr.status));
+        if ('upload' in xhr) {
+            xhr.upload.onprogress = (event) => {
+                if (event.lengthComputable && progressCallback) {
+                    progressCallback(Math.round(event.loaded / event.total * 100));
                 }
             };
+        }
 
-            if ('upload' in xhr) {
-                xhr.upload.onprogress = (event) => {
-                    if (event.lengthComputable && progressCallback) {
-                        progressCallback(Math.round(event.loaded / event.total * 100));
-                    }
-                };
-            }
+        xhr.send(fd);
+    });
+}
+
+// Delete an image from Imgur using its deletehash.
+export function deleteImage(deletehash: string): Promise<boolean> {
+    return new Promise((resolve, reject) => {
+        const xhr = new XMLHttpRequest();
+        xhr.open('DELETE', `https://api.imgur.com/3/image/${deletehash}`, true);
+        setAuthHeader(xhr);
+        xhr.setRequestHeader('Accept', 'application/json');
 
-            xhr.send(fd);
+        xhr.onload = () => {
+            if (xhr.status === 200) {
+                resolve(true);
+            } else {
+                reject(new Error(`Delete failed: HTTP ${xhr.status}`));
+            }
         };
 
-        reader.readAsDataURL(file);
+        xhr.send(null);
     });
 }
diff --git a/src-svelte/src/lib/stores/connectionManager.ts b/src-svelte/src/lib/stores/connectionManager.ts
index 534bb76..21f23bc 100644
--- a/src-svelte/src/lib/stores/connectionManager.ts
+++ b/src-svelte/src/lib/stores/connectionManager.ts
@@ -138,7 +138,7 @@ export async function connect(host: string, port: number, path: string, password
                     });
                     const hlResp = await sendAsync(hlMsg);
                     handleHotlistInfo(hlResp);
-                }, 60000);
+                }, 10000);
 
                 resolve();
             } catch (e) {
diff --git a/src-svelte/src/lib/stores/handlers.ts b/src-svelte/src/lib/stores/handlers.ts
index f5eab63..3b2f9d8 100644
--- a/src-svelte/src/lib/stores/handlers.ts
+++ b/src-svelte/src/lib/stores/handlers.ts
@@ -255,7 +255,11 @@ export function handleBufferLineAdded(message: ProtocolMessage) {
                 }
             } else {
                 // After sync: increment lastSeen so readmarker stays in place.
-                if (buffer.lastSeen >= 0) {
+                // Only shift lastSeen for the ACTIVE buffer — new messages appear below
+                // the readmarker as you scroll. For inactive buffers, don't shift lastSeen;
+                // instead let unread track unread messages so they appear below the marker
+                // when you switch back to this buffer.
+                if (buffer.lastSeen >= 0 && buffer.id === activeId) {
                     buffer.lastSeen++;
                 }
 
@@ -508,19 +512,11 @@ export function handleHotlistChanged(message: ProtocolMessage) {
     const [hotlistPointers] = content;
     const currentBuffers = get(buffers);
     const currentServers = get(servers);
-    // Reset all non-active buffers before applying hotlist entries
-    for (const id in currentBuffers) {
-        const buf = currentBuffers[id];
-        if (buf && !buf.active) {
-            buf.unread = 0;
-            buf.notification = 0;
-        }
-    }
-    for (const key in currentServers) {
-        const srv = currentServers[key];
-        if (srv) srv.unread = 0;
-    }
-    // Apply hotlist entries from WeeChat — mirror original AngularJS behavior
+    // Apply hotlist entries from WeeChat — update unread/notification counts
+    // but do NOT recalculate lastSeen. lastSeen is tracked locally per-buffer
+    // (incremented on active buffer, preserved on inactive), and recalculating
+    // it from hotlist counts would produce wrong values when local line count
+    // has grown since WeeChat computed its hotlist.
     if (hotlistPointers) {
         const pointers = Array.isArray(hotlistPointers) ? hotlistPointers : [hotlistPointers];
         for (const bufferId of pointers) {
@@ -532,11 +528,6 @@ export function handleHotlistChanged(message: ProtocolMessage) {
                 const counts = (entry.content as any[])[0]?.count || [0, 0, 0, 0];
                 buffer.unread = counts[1] || 0;
                 buffer.notification = (counts[2] || 0) + (counts[3] || 0);
-                const unreadSum = counts.reduce((sum: number, n: number) => sum + n, 0);
-                buffer.lastSeen = buffer.lines.length - 1 - unreadSum;
-            } else {
-                buffer.unread = 0;
-                buffer.notification = 0;
             }
         }
     }
@@ -557,17 +548,11 @@ export function handleHotlistInfo(message: ProtocolMessage) {
         const buffer = currentBuffers[entry.buffer];
         if (!buffer || buffer.active) continue;
 
-        // Store unread count from WeeChat hotlist.
-        // During initial sync (buffer has no lines), defer lastSeen calculation
-        // until after sync completes, when we know the total line count.
+        // Set unread/notification counts from WeeChat hotlist. Do NOT set lastSeen —
+        // it is tracked locally: incremented on active buffer during real-time messages,
+        // and calculated during sync completion via handleBufferLineAdded's sync logic.
         buffer.unread = entry.count[1] || 0;
         buffer.notification = (entry.count[2] || 0) + (entry.count[3] || 0);
-        // Only calculate lastSeen if buffer already has lines (post-sync).
-        // During initial sync, buffer.lines.length === 0 so this would be wrong.
-        if (buffer.lines.length > 0) {
-            const unreadSum = entry.count.reduce((sum: number, n: number) => sum + n, 0);
-            buffer.lastSeen = buffer.lines.length - 1 - unreadSum;
-        }
 
         const serverKey = `${buffer.plugin}.${buffer.server}`;
         const server = currentServers[serverKey];
diff --git a/src-svelte/src/lib/stores/models.ts b/src-svelte/src/lib/stores/models.ts
index f0eb984..a684af2 100644
--- a/src-svelte/src/lib/stores/models.ts
+++ b/src-svelte/src/lib/stores/models.ts
@@ -259,9 +259,6 @@ export const bufferScrollPositions = writable<Record<string, number>>({});
 // Tracks buffer line count at last visit (for accurate lastSeen calculation)
 export const bufferLineCounts = writable<Record<string, number>>({});
 
-// Tracks which buffers have local-only unread messages (not reported by WeeChat).
-// Used by handleHotlistChanged to avoid overwriting correct lastSeen with stale WeeChat data.
-export const localUnreadBuffers = writable<Set<string>>(new Set());
 
 // Track whether we're in the initial post-connect sync phase.
 // During this phase, lastSeen is not updated per-line — it's calculated
@@ -343,27 +340,22 @@ export function setActiveBuffer(bufferId: string): boolean {
     const buffer = currentBuffers[bufferId];
     if (!buffer) return false;
 
-  const prevId = get(activeBufferId);
+    const prevId = get(activeBufferId);
     if (prevId && currentBuffers[prevId]) {
         const prev = currentBuffers[prevId];
         prev.active = false;
         // Save line count when leaving this buffer (for lastSeen fallback on return).
-        // Do NOT modify prev.lastSeen — let handleHotlistInfo / handleLineInfo manage it.
+        // Do NOT modify prev.lastSeen — it is maintained by handleBufferLineAdded.
         const bufCopy = { ...prev };
         bufCopy.lines = [...prev.lines];
         bufferLineCounts.update(counts => ({ ...counts, [prevId]: bufCopy.lines.length }));
         previousBufferId.set(prevId);
     }
 
-    // Save local unread count before clearing (tracks ALL messages while non-active,
-    // not just notify_level=1 — needed when WeeChat already displayed the message).
-    const localUnread = buffer.unread;
-
-    // Calculate lastSeen for the incoming buffer using our local unread count.
-    // This is more accurate than WeeChat hotlist because WeeChat may have already
-    // displayed the message (e.g., gbbot sends via /msg in WeeChat itself).
-    if (localUnread > 0 && buffer.lines.length > 0) {
-        buffer.lastSeen = Math.max(0, buffer.lines.length - localUnread - 1);
+    // If lastSeen was never set (e.g., buffer received messages during sync
+    // but sync completion didn't fire for it), calculate from local unread counts.
+    if (buffer.lastSeen < 0 && (buffer.unread + buffer.notification) > 0) {
+        buffer.lastSeen = Math.max(0, buffer.lines.length - (buffer.unread + buffer.notification) - 1);
     }
 
     buffer.active = true;
diff --git a/src-svelte/test/unit/imgur.test.ts b/src-svelte/test/unit/imgur.test.ts
index 3febd5e..9e656ce 100644
--- a/src-svelte/test/unit/imgur.test.ts
+++ b/src-svelte/test/unit/imgur.test.ts
@@ -12,12 +12,14 @@ vi.mock('$lib/stores/settings', () => ({
 // Track the actual FileReader instance created
 let actualReader: {
     onload: ((e: unknown) => void) | null;
+    onerror: ((e: unknown) => void) | null;
     readAsDataURL: ReturnType<typeof vi.fn>;
 };
 
 const mockFileReader = vi.fn(() => {
     actualReader = {
         onload: null,
+        onerror: null,
         readAsDataURL: vi.fn(),
     };
     return actualReader;
@@ -88,6 +90,36 @@ describe('uploadImage', () => {
         expect(res.deletehash).toBe('dh123');
     });
 
+    it('accepts a base64 data URL directly without FileReader', async () => {
+        const { uploadImage } = await import('$lib/imgur');
+        const result = uploadImage('data:image/png;base64,abc123', () => {});
+
+        expect(mockFileReader).not.toHaveBeenCalled();
+        expect(actualXhr.open).toHaveBeenCalledWith('POST', 'https://api.imgur.com/3/image', true);
+
+        actualXhr.status = 200;
+        actualXhr.responseText = JSON.stringify({ data: { link: 'http://i.imgur.com/direct.png', deletehash: 'dh456' } });
+        actualXhr.onload!.call(actualXhr);
+
+        const res = await result;
+        expect(res.link).toBe('https://i.imgur.com/direct.png');
+    });
+
+    it('accepts a raw base64 string (no data URI prefix)', async () => {
+        const { uploadImage } = await import('$lib/imgur');
+        const result = uploadImage('rawbase64string', () => {});
+
+        expect(mockFileReader).not.toHaveBeenCalled();
+        expect(actualXhr.open).toHaveBeenCalled();
+
+        actualXhr.status = 200;
+        actualXhr.responseText = JSON.stringify({ data: { link: 'http://i.imgur.com/raw.png', deletehash: 'dh789' } });
+        actualXhr.onload!.call(actualXhr);
+
+        const res = await result;
+        expect(res.link).toBe('https://i.imgur.com/raw.png');
+    });
+
     it('uses client ID when no iToken configured', async () => {
         const { uploadImage } = await import('$lib/imgur');
         const file = new File(['test'], 'test.png', { type: 'image/png' });
@@ -95,7 +127,7 @@ describe('uploadImage', () => {
         actualReader.onload!({ target: { result: 'data:image/png;base64,abc' } } as unknown as ProgressEvent);
         actualXhr.status = 200;
         actualXhr.responseText = JSON.stringify({ data: { link: 'http://imgur.com/img', deletehash: 'dh' } });
-        actualXhr.onload!();
+        actualXhr.onload!.call(actualXhr);
 
         expect(actualXhr.setRequestHeader).toHaveBeenCalledWith('Authorization', 'Client-ID 164efef8979cd4b');
     });
@@ -108,7 +140,7 @@ describe('uploadImage', () => {
 
         actualXhr.status = 401;
         actualXhr.responseText = JSON.stringify({ status: 401, data: null });
-        actualXhr.onload!();
+        actualXhr.onload!.call(actualXhr);
 
         await expect(result).rejects.toThrow('Upload failed');
     });
@@ -121,7 +153,7 @@ describe('uploadImage', () => {
 
         actualXhr.status = 200;
         actualXhr.responseText = JSON.stringify({ data: {} });
-        actualXhr.onload!();
+        actualXhr.onload!.call(actualXhr);
 
         await expect(result).rejects.toThrow('Upload failed');
     });
@@ -155,8 +187,50 @@ describe('uploadImage', () => {
         await expect(uploadImage(file, () => {})).rejects.toThrow('not an image');
     });
 
-    it('rejects undefined file', async () => {
+    it('rejects null file', async () => {
         const { uploadImage } = await import('$lib/imgur');
-        await expect(uploadImage(null as unknown as File, () => {})).rejects.toThrow('not an image');
+        await expect(uploadImage(null as any, () => {})).rejects.toThrow('not an image');
     });
 });
+
+describe('deleteImage', () => {
+    beforeEach(() => {
+        vi.clearAllMocks();
+        actualXhr = {
+            open: vi.fn(),
+            setRequestHeader: vi.fn(),
+            send: vi.fn(),
+            status: 0,
+            responseText: '',
+            onload: null,
+            upload: {
+                onprogress: null,
+            },
+        };
+    });
+
+    it('deletes an image successfully', async () => {
+        const { deleteImage } = await import('$lib/imgur');
+        const result = deleteImage('dh123');
+
+        expect(actualXhr.open).toHaveBeenCalledWith('DELETE', 'https://api.imgur.com/3/image/dh123', true);
+        expect(actualXhr.setRequestHeader).toHaveBeenCalledWith('Authorization', 'Client-ID 164efef8979cd4b');
+
+        actualXhr.status = 200;
+        actualXhr.onload!.call(actualXhr);
+
+        const res = await result;
+        expect(res).toBe(true);
+    });
+
+    it('rejects on API error', async () => {
+        const { deleteImage } = await import('$lib/imgur');
+        const result = deleteImage('dh456');
+
+        actualXhr.status = 403;
+        actualXhr.responseText = JSON.stringify({ status: 403, data: null });
+        actualXhr.onload!.call(actualXhr);
+
+        await expect(result).rejects.toThrow('Delete failed');
+    });
+});
\ No newline at end of file
